[project]
name = "campylaspis"
version = "0.1.0"
description = "Pacote para pesquisa — estrutura inicial."
authors = [ { name = "Seu Nome", email = "you@example.com" } ]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = [
  "numpy>=1.25",
  "pandas>=2.0",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
